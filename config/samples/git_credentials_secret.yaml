# Git credentials secret for FluxUp UpgradeRequest controller
# Replace ${GITEA_TOKEN} with your actual token
apiVersion: v1
kind: Secret
metadata:
  name: fluxup-git-credentials
  namespace: fluxup-system
type: Opaque
stringData:
  token: "${GITEA_TOKEN}"
