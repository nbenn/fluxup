---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: managedapps.fluxup.dev
spec:
  group: fluxup.dev
  names:
    kind: ManagedApp
    listKind: ManagedAppList
    plural: managedapps
    singular: managedapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='UpdateAvailable')].status
      name: Update
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedApp is the Schema for the managedapps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedAppSpec defines the desired state of ManagedApp
            properties:
              autoRollback:
                default: false
                description: |-
                  AutoRollback enables automatic rollback when upgrades fail after Git commit.
                  When enabled, a RollbackRequest is automatically created if the upgrade fails
                  after the point of no return (Git commit).
                type: boolean
              gitPath:
                description: |-
                  Path to the manifest in Git (for matching Renovate output and editing)
                  Examples: "flux/apps/gitea/helmrelease.yaml", "flux/apps/bazarr/deployment.yaml"
                type: string
              healthCheck:
                description: Health check configuration
                properties:
                  timeout:
                    default: 5m
                    description: Timeout for health checks
                    type: string
                type: object
              helmReleaseRef:
                description: |-
                  HelmReleaseRef references a HelmRelease for PVC and workload discovery.
                  If set, the controller will discover RWO PVCs and workloads from the Helm release.
                  If not set, the controller will attempt to discover from the Kustomization's inventory.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              kustomizationRef:
                description: Reference to the Flux Kustomization that reconciles this
                  app (for suspend/resume)
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              suspendRef:
                description: |-
                  SuspendRef specifies which Kustomization to suspend during upgrades/rollbacks.
                  This is required for app-of-apps patterns where a parent Kustomization manages
                  child Kustomizations. If not set, defaults to KustomizationRef.
                  The referenced Kustomization must be a "root" - not managed by another Kustomization.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              versionPolicy:
                description: Version policy for updates
                properties:
                  autoUpdate:
                    default: none
                    enum:
                    - none
                    - patch
                    - minor
                    - major
                    type: string
                  versionPath:
                    description: |-
                      VersionPath is the YAML path to the version field in the manifest.
                      For HelmRelease, defaults to ".spec.chart.spec.version".
                      For other resources, must be specified explicitly.
                    type: string
                type: object
              volumeSnapshots:
                description: Volume snapshot configuration
                properties:
                  enabled:
                    description: Enabled enables volume snapshots before upgrades
                    type: boolean
                  excludePVCs:
                    description: |-
                      ExcludePVCs is a list of PVC names to exclude from auto-discovery.
                      Useful for excluding ephemeral volumes like caches.
                    items:
                      description: PVCRef references a PVC for snapshotting
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  pvcs:
                    description: |-
                      PVCs is an explicit list of PVCs to snapshot. If not specified,
                      the controller will auto-discover RWO PVCs from the app's workloads.
                    items:
                      description: PVCRef references a PVC for snapshotting
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  retentionPolicy:
                    description: RetentionPolicy defines how many snapshots to keep
                      per PVC
                    properties:
                      maxCount:
                        default: 3
                        description: |-
                          MaxCount is the maximum number of snapshots to retain per PVC.
                          After a successful upgrade, older snapshots beyond this count are pruned.
                          Defaults to 3 if not specified. Set to 0 to disable retention (keep all).
                        minimum: 0
                        type: integer
                    type: object
                  volumeSnapshotClassName:
                    description: VolumeSnapshotClassName is the name of the VolumeSnapshotClass
                      to use
                    type: string
                required:
                - enabled
                type: object
            required:
            - gitPath
            - kustomizationRef
            type: object
          status:
            description: ManagedAppStatus defines the observed state of ManagedApp.
            properties:
              availableUpdate:
                description: Available update information
                properties:
                  chart:
                    description: Chart version (for HelmRelease)
                    type: string
                  images:
                    description: Image versions (for raw deployments or helm values)
                    items:
                      description: ImageInfo contains image version details
                      properties:
                        name:
                          type: string
                        tag:
                          type: string
                      required:
                      - name
                      - tag
                      type: object
                    type: array
                type: object
              conditions:
                description: conditions represent the current state of the ManagedApp
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentVersion:
                description: Current version information
                properties:
                  chart:
                    description: Chart version (for HelmRelease)
                    type: string
                  images:
                    description: Image versions (for raw deployments or helm values)
                    items:
                      description: ImageInfo contains image version details
                      properties:
                        name:
                          type: string
                        tag:
                          type: string
                      required:
                      - name
                      - tag
                      type: object
                    type: array
                type: object
              lastUpgrade:
                description: Last upgrade information
                properties:
                  fromVersion:
                    description: VersionInfo contains version details
                    properties:
                      chart:
                        description: Chart version (for HelmRelease)
                        type: string
                      images:
                        description: Image versions (for raw deployments or helm values)
                        items:
                          description: ImageInfo contains image version details
                          properties:
                            name:
                              type: string
                            tag:
                              type: string
                          required:
                          - name
                          - tag
                          type: object
                        type: array
                    type: object
                  status:
                    type: string
                  timestamp:
                    format: date-time
                    type: string
                  toVersion:
                    description: VersionInfo contains version details
                    properties:
                      chart:
                        description: Chart version (for HelmRelease)
                        type: string
                      images:
                        description: Image versions (for raw deployments or helm values)
                        items:
                          description: ImageInfo contains image version details
                          properties:
                            name:
                              type: string
                            tag:
                              type: string
                          required:
                          - name
                          - tag
                          type: object
                        type: array
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
